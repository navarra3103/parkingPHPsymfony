<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Editar Coche</title>
    {# He incluido Bootstrap directamente aquí para el estilo #}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    {# Puedes añadir tu styles.css si quieres aplicar estilos adicionales #}
    <link href="{{ asset('css/styles.css') }}" rel="stylesheet" />
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h2 class="mb-4 text-center">Editar Coche: {{ matricula }}</h2>

        {# Inicia el formulario de edición #}
        {{ form_start(formulario_editar_coche) }}
            {# Mostrar los campos del formulario #}
            {{ form_row(formulario_editar_coche.matricula) }}
            {{ form_row(formulario_editar_coche.marca) }}
            {{ form_row(formulario_editar_coche.modelo) }}
            {{ form_row(formulario_editar_coche.color) }}
            {# Botón para guardar los cambios #}
            <button type="submit" class="btn btn-primary">Guardar Cambios</button>
        {{ form_end(formulario_editar_coche) }}

        <div class="text-center mt-3">
            {# Enlace para volver a la lista de coches #}
            <a href="{{ path('app_show_cars') }}" class="btn btn-secondary">← Volver a lista</a>
        </div>
    </div>
</body>
</html>